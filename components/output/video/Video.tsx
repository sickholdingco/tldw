import { useState } from "react";

interface VideoProps {
  videoId: string;
  title: string;
  thumbnail: string;
}

export const Video = ({ videoId, title, thumbnail }: VideoProps) => {

  return (
   <div className="flex flex-col gap-4">
      poop
   </div>
  );
};
